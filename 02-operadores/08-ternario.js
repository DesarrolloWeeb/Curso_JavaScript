// expression ? 'Si es True' : 'Si es False  "Por ejemplo si estuvieramos evaluando si un número es mayor que otro"
let edad = 30;
let acceso = edad > 17 ? 'Permitir el ingreso' : 'No permitir ingreso';
console.log(acceso);